# 2026-05-09 — Crypto Bot Development Session

## 🚀 ADVANCED v5.0 ARCHITECTURE BUILT
Created 8 new files in `agents/` for real-time crypto intelligence:
- `solana_rpc_monitor.py` — Direct Solana RPC reads, <1s latency
- `pump_portal_monitor.py` — WebSocket for Pump.fun launches
- `scraper_engine.py` — Playwright + BeautifulSoup scraping (DexScreener, GMGN, Photon)
- `anti_detection.py` — Anti-bot detection evasion (fingerprint rotation, proxy rotation)
- `ai_evaluation_layer.py` — Multi-dimensional AI analysis (scam probability, social sentiment, momentum, on-chain, volatility)
- `v5_orchestrator.py` — Advanced orchestrator with pipeline: Scanner → Scraper → AI Eval → Signal
- `twitter_scraper.py` — X/Twitter sentiment extraction via Nitter
- `requirements_advanced.txt` — Dependencies for v5 stack

All v5 files placed in `agents/` directory (NOT in `advanced/` subfolder as originally planned — user wanted direct placement).

## 🔧 v4 SYSTEM FIXES (ACTIVE PRODUCTION)
Fixed the live v4 system which the user was complaining about:

### 1. Confidence Threshold Lowered
- Was: confidence >= 60 AND R:R >= 2.0 → only 2 coins approved (FLOKI, BV7X)
- Now: confidence >= 40 AND R:R >= 1.5 → 8 coins approved
- JTO previously had confidence 60+ but market conditions changed and dropped to 40 (now passes)

### 2. Profit-Optimized Tier System
Added to `v2_dynamic_risk.py`:
- `profit_potential`: upside room calculation (0-100)
- `execution_probability`: likelihood of hitting targets (0-100)
- `expected_return_pct`: weighted expected profit
- Tier classification: TIER_1 (exceptional), TIER_2 (good), TIER_3 (moderate), REJECT

Top opportunities found (sorted by profit_potential):
1. Octopus — PP: 88.1, Expected Return: +31.2%, Exec: 85%
2. Bonk — PP: 85.0, ER: +9.0%, Exec: 90%
3. ROAF — PP: 78.1, ER: +9.0%, Exec: 90%
4. MEGR — PP: 73.9, ER: +8.0%, Exec: 80%
5. JUP — PP: 70.0, ER: +9.0%, Exec: 90%
6. SHIB — PP: 60.7, ER: +6.5%, Exec: 65%
7. FLOKI — PP: 55.3, ER: +8.0%, Exec: 80%
8. BV7X — PP: 54.2, ER: +8.0%, Exec: 80%

### 3. Price Formatting Fixed
- Created `format_price()` helper in `v2_dynamic_risk.py`
- Eliminates scientific notation (e.g. `$3.512e-05` → `$0.00003512`)
- Smart rounding: <0.0001 → 8 decimals, <0.01 → 6 decimals, <1 → 4 decimals, <1000 → 2 decimals, >=1000 → K/M formatting
- Applied to v4_master_controller.py signal messages

### 4. Scanner Broadened
- `v2_scanner.py` now fetches boosted tokens + top movers + baseline coverage
- Gets 20-30 tokens per scan instead of 2-3
- Added `get_boosted_tokens()` and `get_top_movers()` functions

## 📊 USER PREFERENCE: MAXIMUM PROFIT DETECTION
User explicitly said: "Θέλω να ανιχνεύει τις καλύτερες επιλογές και πιο κερδοφόρες"
- Wants profit-optimized filtering (not just any signal)
- Prefers fewer, higher-quality signals over noise
- Wants to see expected returns, not just raw data

## 📝 FILES MODIFIED TODAY
- `/root/.openclaw/workspace/agents/v2_dynamic_risk.py` — Profit scoring, tier system, confidence fixes
- `/root/.openclaw/workspace/agents/v2_validator.py` — Threshold lowered, pass rate logic
- `/root/.openclaw/workspace/agents/v2_scanner.py` — Discovery broadened
- `/root/.openclaw/workspace/agents/v4_master_controller.py` — Formatting fixes
- `/root/.openclaw/workspace/agents/v5_orchestrator.py` — New advanced orchestrator
- Plus 7 new files listed above

## ⚠️ KNOWN ISSUES
- v5 files are created but NOT running yet (user said v4 is priority)
- Telegram cross-context messaging blocked from kimi-claw channel
- Paper trading JSON read still has syntax error (not yet fixed)
- User still needs to choose A/B/C/D for signal delivery preference
- Auto mode requires `auto_mode.json` with `"enabled": true` to bypass manual confirmation

## 🎯 NEXT STEPS (PENDING USER INPUT)
1. User needs to choose signal delivery option: A=all 8, B=top 3, C=tier 1 only, D=stricter
2. If no response in 2 min, default to B (top 3: Octopus, Bonk, ROAF)
3. After that: run live scan and deliver signals via Telegram
