# 🔮 Polymarket AI Trading Bot — 2026-05-31

**Status:** ✅ BUILT & READY
**Location:** `/root/.openclaw/workspace/agents/`
**Files:**
- `polymarket_brain.py` — Signal engine + edge calculator + Kelly + risk manager
- `polymarket_scanner.py` — Gamma API scanner
- `polymarket_alerts.py` — Telegram alerts
- `run_polymarket_brain.py` — Runner script

**Architecture:**
- SignalGenerator: momentum (5m/15m/1h), volume anomaly, order book imbalance, sentiment
- EdgeCalculator: fair value vs market price with confidence
- KellyCriterion: fractional Kelly (1/2 base) with dynamic adjustment
- RiskManager: position limits, circuit breakers, correlation, exposure, SQLite persistence
- DynamicRiskProfile: aggressive → moderate → paused based on loss streak
- PolymarketBrain: orchestrator with continuous scan loop

**Risk Rules:**
- Base Kelly: 50%
- Max position: 5% bankroll
- Max daily loss: 50% bankroll
- Max exposure: 30% bankroll
- After 3 losses: reduce to 25% Kelly
- After 5 losses: pause 1 hour
- Near expiry (<6h): reduce 50%

**Signal Score:**
```python
signal_score = momentum_score * 0.4 + volume_anomaly * 0.3 + order_book_imbalance * 0.2 + sentiment_score * 0.1
edge = abs(fair_value - market_price)
```

**Next:** Core trading engine (order execution) from another worker


---

# 🎯 Stoiximan Cashout Strategy — Desktop Application Build (2026-05-31)

**Status:** ✅ BUILT & READY
**Location:** `/root/.openclaw/workspace/apps/cashout_strategy/`
**User Demand:** "Πραγματικά API και world OSINT football bets prices live"

## Architecture Built

### 1. Core Engine (`app.py`)
- `Database` — SQLite for opportunities & performance tracking
- `PinnacleMonitor` — Sharp bookmaker odds + dropping odds detection (>5% threshold)
- `StoiximanMonitor` — Soft bookmaker odds + Golden Hour detection (60-90 min window)
- `OpportunityEngine` — Two-Step Cashout logic:
  1. Detect Pinnacle dropping odds (smart money signal)
  2. Check if Stoiximan hasn't adjusted yet
  3. Calculate value edge + confidence score (0-100)
  4. Flag for pre-match cashout during Golden Hour
- `CashoutApp` — Main controller with asyncio loop, supports demo/live mode toggle
- **OSINT Integration:** `_run_osint()` per opportunity, `osint_reports` dict

### 2. Real API Integrations

#### Pinnacle API (`integrations/pinnacle_api.py`)
- `PinnacleAPI` — Authenticated v3 API with Basic Auth, rate limiting, retry logic
- `PinnacleDemoAPI` — Demo mode with realistic Greek league data
- Methods: `get_sports()`, `get_leagues()`, `get_fixtures()`, `get_odds()`, `parse_odds_data()`, `detect_odds_drops()`
- Soccer sport ID: 29

#### Stoiximan API (`integrations/stoiximan_api.py`)
- `StoiximanAPI` — Public odds API with browser headers, 2s rate limit
- `StoiximanDemoAPI` — Demo mode with HIGHER odds than Pinnacle (simulating value window)
- Methods: `get_sports()`, `get_leagues()`, `get_matches()`, `get_match_odds()`, `get_all_odds()`, `is_golden_hour()`
- Soccer sport ID: 1

#### Football OSINT (`osint/football_osint.py`)
- `TransfermarktScraper` — Injuries, squad form (demo data for Greek + English teams)
- `FlashscoreAPI` — Live scores, H2H stats
- `BettingSentimentAnalyzer` — Sharp money direction, public bias, confidence scoring
- `OddscheckerAPI` — Best odds across all bookmakers
- `FootballOSINT` — Orchestrator generating `IntelligenceReport` objects:
  - INJURY, FORM, SENTIMENT, ODDS_MOVEMENT types
  - Confidence scores 65-90

### 3. Dashboard (`dashboard/`)
- **Backend:** `server.py` — FastAPI with endpoints:
  - `GET /` — Dashboard UI
  - `GET /api/dashboard` — Full data
  - `POST /api/start|stop` — App control
  - `POST /api/settings` — Configuration
  - `POST /api/opportunities/{id}/execute` — Mark executed
  - `GET /api/opportunities` — List with status filter
  - `GET /guide` — Serve .docx strategy guide
- **Frontend:** `templates/index.html` + `static/style.css` + `static/dashboard.js`
- **UI Features:**
  - Dark theme, responsive design
  - 6 stat cards (total, executed, profit %, win rate, today's, active)
  - Live opportunities table with status badges, confidence bars, Golden Hour highlighting
  - Two-Step Strategy visual guide panel
  - Settings panel (scan interval, min drop %, min confidence, bankroll, max stake %, Telegram alerts toggle)
  - Activity log with auto-scroll
  - Auto-refresh every 30s, WebSocket ready

### 4. Desktop Integration
- `launch.sh` — Start/stop/status/restart/install commands
- Auto-finds free port, opens browser via `xdg-open`/`open`
- `Stoiximan-Cashout-Pro.desktop` — Desktop entry for Linux
- `requirements.txt` — FastAPI, uvicorn, aiohttp, requests, websockets

### 5. Strategy Guide
- Full 10,424-word docx guide from Kimi's research: `downloads/19e7b63a-1a42-8104-8000-00002bce483f_Stoiximan_Cashout_Strategy_Guide.docx`
- Two-Step Cashout strategy: Pinnacle → Stoiximan → Pre-match cashout
- Key metrics: 5.8% edge on dropping odds, 75% positive CLV
- Risk: Account gubbing after 3-12 months, mug bets for camouflage

## Key Commands
```bash
cd /root/.openclaw/workspace/apps/cashout_strategy
./launch.sh install   # deps
./launch.sh start     # server + browser
./launch.sh status    # check PID
./launch.sh stop      # kill
```

## Dashboard URL
http://localhost:8080 (auto-adjusts if port taken)

## Remaining Integration Points
- Replace demo APIs with real credentials in `app.py`: `use_demo=False`
- Add Telegram alert webhook to `app.py` OSINT results
- Connect live Pinnacle API key via env vars: `PINNACLE_API_KEY`, `PINNACLE_USERNAME`, `PINNACLE_PASSWORD`
- WebSocket real-time updates in `dashboard.js` (commented out, needs server endpoint)
- Stoiximan real scraping may need session/cookie handling beyond current public API attempt

---

---

# 🤖 POLYMARKET AI SIGNAL ENGINE — Phase 1 COMPLETE (2026-05-31)
**Status:** ✅ BUILT & READY FOR TESTING
**Location:** `/root/.openclaw/workspace/polymarket_bot/`
**Mode:** Paper Trading (testnet Amoy ready)
**Risk Level:** Dynamic (auto-adjusts based on drawdown)

### Files Created (10 files, ~80KB total):
| File | Size | Purpose |
|------|------|---------|
| `config.py` | ~5KB | All configuration — APIs, risk, execution, DB, Telegram |
| `risk_manager.py` | ~9KB | Kelly Criterion, drawdown protection, circuit breakers |
| `signal_engine.py` | ~25KB | 5-factor AI signal generation |
| `market_scanner.py` | ~16KB | Polymarket Gamma/CLOB/Data API integration |
| `decision_engine.py` | ~20KB | Trade decisions, position management, exits |
| `database.py` | ~18KB | SQLite tracking (signals, trades, positions, performance) |
| `telegram_alerts.py` | ~10KB | Real-time Telegram alerts |
| `main.py` | ~13KB | 24/7 event loop, orchestrates all components |
| `requirements.txt` | ~350B | Python dependencies |
| `README.md` | ~11KB | Full documentation |

### Architecture:
```
Market Scanner → Signal Engine → Decision Engine → Execution
                    ↑                ↓
              Risk Manager ← Position Manager ← Database
                    ↓
              Telegram Alerts
```

### Signal Engine — 5 Factor AI Scoring:
| Factor | Weight | Description |
|--------|--------|-------------|
| **Momentum** | 35% | Price trends 5m/15m/1h/4h |
| **Liquidity** | 25% | Spread, order book depth, volume |
| **Sentiment** | 20% | Order book imbalance, volume trends |
| **Technical** | 15% | RSI, Bollinger Bands, Moving Averages |
| **Fundamental** | 5% | Time to resolution, tags, consistency |

**Confluence bonus:** +15 points when 3+ factors agree.

### Risk Management:
- **Kelly Criterion:** Base 25% of full Kelly, dynamic adjustment
- **Circuit Breakers:** -20% daily loss → 1h pause, -35% → 4h pause, -50% → 24h pause
- **Dynamic Risk:** Adjusts position sizes based on drawdown (10 levels)
- **Exit Strategy:** Stop Loss -20%, TP1 +50%, TP2 +100%, TP3 +200%

### APIs Integrated:
- **Gamma API** — Market discovery (`https://gamma-api.polymarket.com`)
- **CLOB API** — Order book + execution (`https://clob.polymarket.com`)
- **Data API** — Historical data (`https://data-api.polymarket.com`) — ⚠️ HTTP 404, not accessible
- **WebSocket** — Real-time feeds (`wss://ws-subscriptions-clob.polymarket.com`)

### Gamma API Parsing Fixes Applied:
- `order=desc` parameter → HTTP 422, removed
- Response format: `list` not `dict` with `markets` key
- `liquidity` field: `str` type, needs `float()` conversion
- `volume24hr` field: `float` type (works directly)
- `clobTokenIds`: comma-separated string (not `tokens` array)
- `outcomePrices`: JSON-encoded string, needs `json.loads()`

### Market Scanner Price Sources:
- `lastTradePrice` — most recent trade price
- `bestBid` / `bestAsk` — order book top of book
- `oneHourPriceChange`, `oneDayPriceChange`, `oneWeekPriceChange`, `oneMonthPriceChange` — historical changes

### Live Scan Results:
- 20 real markets fetched → 9 signals generated
- Scores: 61-63% (conservative, good for start)
- Momentum scores flat (~50) due to lack of granular OHLC data
- Data API unavailable — fallback to Gamma API built-in price change fields

### Telegram Alerts:
- 🔥 Signal alerts (confidence + factor breakdown)
- ✅ Trade execution alerts
- 🛑 Stop loss / 🎯 Take profit alerts
- ⚠️ Drawdown warnings
- 📊 Daily performance summaries

### Testing Performed:
1. ✅ Batch scan test: 5 markets → 3 signals
2. ✅ Live API test: Gamma API + CLOB API connected
3. ✅ Live scan: 20 markets → 9 signals
4. ✅ Paper trading: Positions opened with $100 balance
5. ✅ Integration: Signal Engine → Trading Engine connected

### Dashboard Built:
- Location: `/root/.openclaw/workspace/polymarket_bot/dashboard/`
- `index.html` — React-style dark UI (P&L, positions, trades, charts)
- `dashboard_server.py` — FastAPI server with SQLite endpoints
- `package.json` — npm config for dashboard
- Features: Real-time charts, position tracking, signal log, activity log

### Security Alert:
- **Compromised Key:** Private key `6264e4d...77122a4b` sent in group chat — MUST ROTATE
- **Bot uses placeholder:** `POLYMARKET_PRIVATE_KEY=your_key_here` in code
- **User must:** Export NEW key from Polymarket, place in local `.env` only
- **Never share private keys in chat**

### Next Steps:
1. **Test in paper mode** — `cd polymarket_bot && python main.py`
2. **Add `.env` with credentials** (locally, never in chat)
3. **Connect Core Trading Engine** — py-clob-client-v2 for live execution
4. **Build Dashboard** — React/Web UI for P&L, positions, market scanner
5. **Switch to live** — After testnet validation

---

# 📊 POLYMARKET AI DASHBOARD — NEW! 2026-05-31
**Status:** ✅ BUILT & READY
**Location:** `/root/.openclaw/workspace/polymarket_bot/dashboard/`
**Stack:** HTML + Tailwind CSS + Chart.js + Font Awesome

**Components:**
1. P&L Panel — Real-time profit/loss metrics (total, daily, unrealized, bankroll)
2. Positions Table — Open positions with direction, entry, current price, P&L
3. Signal Log — All signals with filters (long/short/high score)
4. Trade History — Complete execution history
5. Performance Charts — Equity curve + drawdown (Chart.js)
6. Risk Metrics — Win rate, total trades, open positions
7. Activity Log — Terminal-style activity feed
8. Mode Toggle — Paper/Live mode switch

**Server:**
- `dashboard_server.py` — FastAPI server (port 8080)
- Endpoints: `/api/status`, `/api/positions`, `/api/signals`, `/api/trades`, `/api/performance`
- SQLite database integration
- Static file serving for HTML/CSS/JS

**To run:**
```bash
cd /root/.openclaw/workspace/polymarket_bot
python dashboard_server.py
# Open http://localhost:8080
```

---

# 🔴 SECURITY ALERT — Compromised Private Key (2026-05-31)
**Status:** ⚠️ COMPROMISED — MUST ROTATE
**Key Hash:** `6264e4d...77122a4b` (first 8 + last 8 chars)
**Source:** Sent in group chat by user — visible to all members

### Immediate Action Required:
1. **Export NEW private key** from Polymarket (Cash → Export Private Key)
2. **Transfer funds** from old wallet to new wallet
3. **Never share private key in chat again** — only use local `.env` file
4. **All bot code uses placeholder** (`POLYMARKET_PRIVATE_KEY=your_key_here`)

### Bot Security Model:
- Private keys stored **locally only** in `.env` (never in repo/chat)
- Code uses placeholder — user fills in locally
- Fernet AES-128 encryption available for key storage
- Auto-delete sensitive messages configured

---

# 📈 STOCK TRADING BOT — IBKR Platform (2026-05-31)
**Status:** ✅ BUILT & READY
**Location:** `/root/.openclaw/workspace/downloads/` (delivered as zip + individual files)
**Broker:** Interactive Brokers (IBKR) — Paper Trading Port 7497

### Files Delivered (11+ files):
**Core Engine:**
- `ibkr_client.py` — Connection to TWS/Gateway
- `order_executor.py` — Market/limit/stop/bracket orders
- `position_tracker.py` — P&L, drawdown, portfolio metrics
- `market_data.py` — Real-time ticks + historical bars
- `connection.py` — IBKR connection manager (singleton, auto-reconnect)
- `orders.py` — Order execution + fill monitoring
- `tracker.py` — Position tracking + portfolio summary
- `logging_config.py` — Structured JSON logging
- `main.py` — Trading engine orchestrator
- `test_trading_engine.py` — Unit tests

**AI Brain:**
- `stock_config.py` — Universe (S&P 500 + NASDAQ-100), signal params
- `stock_scanner.py` — Market scanner with yfinance
- `stock_signal_generator.py` — RSI, MACD, Bollinger, EMA, Volume signals
- `stock_risk_manager.py` — Kelly + 2% fixed, drawdown, circuit breakers
- `stock_decision_engine.py` — Trade approval (score >70, edge >2%, R:R >1.5:1)
- `stock_requirements.txt` — Dependencies

**Config:**
- `pyproject.toml`, `requirements.txt`, `README.md`, `.env.template`, `.env.example`

### Strategy:
1. **Scanner** → Every 5 min, scan 50 US stocks (S&P 500)
2. **Signals** → RSI, MACD, Bollinger, EMA → score 0-100
3. **Risk** → Kelly Criterion (1/4), position limits, circuit breakers
4. **Execution** → Limit orders via IBKR, stop-loss/take-profit auto
5. **Monitor** → Position tracking, auto-close on SL/TP

### Risk Rules:
- Position sizing: 2% fixed + Kelly (1/4 fraction)
- Drawdown circuit breaker: 10%
- Daily loss limit: 2%
- Loss streak adjustments: After 3 losses → reduce Kelly to 25%

### Paper Trading Setup:
1. IBKR account → https://www.interactivebrokers.com/
2. Enable Paper Trading (port 7497, free)
3. Download TWS or IB Gateway
4. API permissions: Edit → Global Configuration → API → Enable
5. `cp .env.example .env` → configure
6. `pip install -r requirements.txt`
7. `python main.py` or `python -m trading_engine.main`

### Costs:
- IBKR Paper Trading: €0
- IBKR Live Commissions: $0.005/share ($1 min)
- VPS (24/7): €6-20/month
- Market Data (US): €0 basic or €10-20 premium

### Roadmap (5 Phases):
1. **Phase 1 (Days 1-2):** IBKR account + TWS setup
2. **Phase 2 (Day 3):** Python + bot install + .env config
3. **Phase 3 (Days 4-18):** Paper trading (2+ weeks)
4. **Phase 4 (Day 19+):** Live transition (micro size)
5. **Phase 5 (Day 30+):** VPS deploy + Telegram alerts + Dashboard

### Integration with TradingView:
- TradingView (Pine Script) → Alert → Webhook → Python Bot → IBKR API
- Free tier signals + custom risk management + IBKR execution
- Alternative: PickMyTrade (€29/mo), TradersPost (€19/mo)

---

# 🎯 Polymarket Signal Engine — Live Testing Results (2026-05-31)

### Batch Scan Test:
- 5 markets → 3 signals
- Scores: 60-65% (conservative)
- Liquidity score strong: 84-92
- Momentum moderate: 55

### Live API Test:
- Gamma API: ✅ OK — 5 real markets fetched
- CLOB API: ✅ OK
- Data API: ❌ HTTP 404 (all endpoints)

### Live Scan (20 Markets → 9 Signals):
Sample markets analyzed:
- "Will Jesus Christ return before GTA VI?" — Volume: $781,465, Liquidity: $21,030
- "Will bitcoin hit $1m before GTA VI?" 
- "Carolina Hurricanes win 2026 NHL?"
- "Spain win 2026 FIFA World Cup?"
- Various NBA/NHL/World Cup prediction markets

Signal characteristics:
- Confidence: 61-63%
- Score: 61-63/100
- Momentum: 50 (flat due to lack of OHLC)
- Liquidity: 72-88 (strong)
- Sentiment: 54-59
- Technical: 56
- Fundamental: 62-70

### Paper Trading Test:
- Balance: $100.00
- Positions opened: 2 (paper mode)
- P&L: $0.00 (starting)
- Mode: Paper (no real money)

### Integration Test:
- Signal Engine → Trading Engine: ✅ CONNECTED
- 20 markets scanned → 7 signals → 0 trades (decision engine rejected due to low confidence)
- After fix: 4 trades to execute with 3-4 positions opened
- Pipeline: Market Scanner → Signal Engine → Decision Engine → Trading Engine → Database

---

# 🔧 Technical Fixes Applied (2026-05-31)

### Gamma API Fixes:
1. **HTTP 422** — Removed `order=desc` parameter from `fetch_markets()`
2. **List response** — Added `isinstance(data, list)` check (not dict with `markets` key)
3. **Liquidity type** — `float(market.get("liquidity", 0))` (was string)
4. **Token IDs** — Parse `clobTokenIds` (comma-separated string) instead of `tokens` array
5. **Outcome prices** — `json.loads(outcomePrices)` (was JSON string)
6. **Volume field** — `volume24hr` is float, `volume` is string

### Market Scanner Updates:
- Price fields: `lastTradePrice`, `bestBid`, `bestAsk` (from Gamma API)
- Removed CLOB order book fetch (slow, unnecessary)
- Historical data: `oneDayPriceChange`, `oneHourPriceChange`, `oneWeekPriceChange`, `oneMonthPriceChange`
- Data API fallback: All endpoints return 404, using Gamma API fields only

### Signal Engine Updates:
- `MarketData` dataclass: Added `one_hour_change`, `one_day_change`, `one_week_change`, `one_month_change`
- `analyze_momentum`: Uses new price change fields from Gamma API
- `analyze_liquidity`: `bid_depth` / `ask_depth` default to 0 if None
- `calculate_technical_indicators`: Requires `price_history` length ≥ 5

---

# 📚 Key Decisions Log (2026-05-31)

1. **Dual-track architecture**: Core Trading Engine (Kimi Claw Desktop) + AI Signal Engine (AImind)
2. **Security model**: Private key placeholder in code, user fills `.env` locally
3. **Risk level**: Dynamic (auto-adjusts Kelly fraction and position limits)
4. **Signal formula**: 5-factor weighted scoring + confluence bonus
5. **Kelly Criterion**: Fractional base 0.25 (conservative half-Kelly)
6. **Circuit breakers**: Progressive pauses based on drawdown severity
7. **Exit strategy**: 3 take-profit levels + stop loss
8. **Paper mode default**: `is_live = False` until user explicitly enables
9. **Data API unavailable**: Fallback to Gamma API price change fields
10. **Market scanner**: Uses Gamma API price fields (faster, fewer API calls)

---

# 🚀 Next Actions Pending (2026-05-31)

### Polymarket Bot:
- [ ] User: Create `.env` with NEW private key (rotate compromised key!)
- [ ] User: Add $5-10 USDC to Amoy testnet
- [ ] Test: Run `python main.py` in paper mode
- [ ] Test: Verify signals + positions + database logging
- [ ] Deploy: Dashboard server (`python dashboard_server.py`)
- [ ] Live: Switch to testnet after paper validation
- [ ] Live: Switch to mainnet after testnet success

### Stock Trading Bot:
- [ ] User: Open IBKR account + enable paper trading
- [ ] User: Download TWS or IB Gateway
- [ ] User: Configure `.env` with API credentials
- [ ] Install: `pip install -r requirements.txt`
- [ ] Test: Run paper trading for 2+ weeks
- [ ] Deploy: VPS + 24/7 operation
- [ ] Live: Transition to live trading (micro size first)

### Dashboards:
- [ ] Polymarket: Run `dashboard_server.py` → open http://localhost:8080
- [ ] Stock: Build React dashboard (pending)
- [ ] Both: Telegram alerts integration

---
